@model GetShip.Models.Employe
@{
    GetShip.Models.Selary selary = new GetShip.Models.Selary();
    ViewData["Test"] = "TEST";
}

@foreach (var item in @Model.Selary)
	{
		@item.Count
	}

<h4>@Html.ActionLink("Give salery", "AddSelary", "Company", new { id = @Model }, null)</h4>
<button type="button" class="btn btn-info btn-lg" data-toggle="modal" data-target="#myModal">Open Modal</button>
@using (Html.BeginForm("AddSelary", "Company",FormMethod.Post ))
{ 
<div id="myModal" class="modal fade" role="dialog">
    <div class="modal-dialog">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                
                <h4 class="modal-title">Modal Header</h4>
            </div>
            <div class="modal-body">
                Count:<p class="modal-open">@Html.Editor("Method",selary.Count)</p>
                Date:<p class="modal-open">@Html.Editor("Method", selary.Date)</p>
            </div>
            <div class="modal-footer">
                <input type="submit" value="Text">
            </div>
        </div>

    </div>
</div>
}